import { mkdirSync, readFileSync, writeFileSync, existsSync } from 'node:fs'; import { join } from 'node:path'; import { AgentState, MemoryEvent } from './types.js'; export class LocalStore { constructor(private baseDir: string){mkdirSync(baseDir,{recursive:true});} private p(f:string){return join(this.baseDir,f);} loadAgents():Record<string,AgentState>{const f=this.p('agents.json'); return existsSync(f)?JSON.parse(readFileSync(f,'utf8')):{};} saveAgents(v:Record<string,AgentState>){writeFileSync(this.p('agents.json'),JSON.stringify(v,null,2));} loadEvents(agentId:string):MemoryEvent[]{const f=this.p(`${agentId}.events.json`); return existsSync(f)?JSON.parse(readFileSync(f,'utf8')):[];} saveEvents(agentId:string,e:MemoryEvent[]){writeFileSync(this.p(`${agentId}.events.json`),JSON.stringify(e,null,2));}}
